public class UserRepository implements IUserRepository {
    // This class can implement additional methods if needed
    // For example, you can add custom query methods here

    @Override
    public void save(Schedule schedule) {
        // Implement the logic to save a Schedule object
        // This might involve converting Schedule to User or handling it differently
        // depending on your application's requirements.
    }
}