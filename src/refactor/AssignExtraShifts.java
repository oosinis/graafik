package refactor;

import java.util.List;

import objects.Shift;
import objects.Worker;

public class AssignExtraShifts {
    public static void AddExtraShifts(Shift[][] scheduleMatrix, int daysInMonth, List<Worker> workers) {

    }

    private List<Worker> FilterWorkers(List<Worker> workers) {
        var negativeWorkers = workers.stream().filter(w -> w.getWo)

        List<Person> beerDrinkers = persons.stream()
    .filter(p -> p.getAge() > 16).collect(Collectors.toList());
    }
}
